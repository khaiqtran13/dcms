chetan-server:
	code .;cd packages/server; npm install; npm start

chetan-web:
	code .;cd packages/web; npm install; npm start

init-server:
	cd packages/server; npm install; npm start

init-web:
	cd packages/web; npm install; npm start